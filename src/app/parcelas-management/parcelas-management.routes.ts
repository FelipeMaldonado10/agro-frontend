import { Routes } from '@angular/router';
import { ParcelasListComponent } from './parcelas-list.component';

export const parcelasManagementRoutes: Routes = [
  { path: 'parcelas', component: ParcelasListComponent }
];
